// Optional interactivity — can be expanded
console.log("Wovenbrands site loaded");
// Wait until page fully loads, then fade in
window.addEventListener("load", () => {
  document.body.classList.add("loaded");
});
